pipeline {
  agent any
  stages {
    stage('Example') {
      steps {
        git branch: "${params.BRANCH}", url: 'https://github.com/Alex-Sergeev/jenkins-pipe-test.git'
      }
    }
	
	stage('Test') {
      steps {
        sh "ls -la"
      }
    }
  }
}